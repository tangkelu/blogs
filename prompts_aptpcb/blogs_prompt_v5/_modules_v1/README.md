# APTPCB prompt modules (v1)

These are **optional prompt modules** that add a content angle or extra constraints on top of the 3 base skeletons:
- base skeletons: `query` / `pillar` / `playbook`
- modules: `comparison` / `application` / `capability`

The generator can inject modules automatically (based on keyword intent), or you can force modules via CLI flags.

