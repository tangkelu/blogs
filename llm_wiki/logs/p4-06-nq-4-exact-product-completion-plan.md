# P4-06 NQ-4 Exact-Product Completion Plan

Date: 2026-04-26

## Purpose

This note narrows `NQ-4` from `logs/p4-06-safe-wave-draft-feasibility-and-high-density-gap-queue.md` into a bounded long-task plan.

It is not a generic reopening of `H1`.

It is a controlled exact-product supplementation pass for the safe-wave layer-count program.

## Current Read Of The Queue

Multi-agent discovery shows that `NQ-4` is narrower than the queue label first suggests.

The current actionable work falls into three lanes:

1. `FR-4` family-shortcut cleanup where safe-wave packs still rely on family examples without a reusable exact-product unit
2. flex-material exact-product normalization where the repo already has usable product anchors beyond the older Panasonic-first wording
3. remaining safe-wave named-product gap scan, but only for names that materially improve current evidence packs rather than broadening held branches

Current conclusion:

- `NQ-4` is partly a new-card queue
- but it is also a normalization and pack-integration queue
- a large share of apparent gaps should remain held or be resolved without creating new cards

## Lane A: FR-4 Family-Shortcut Cleanup

Current primary candidates:

1. `Isola IS410`
2. `Isola 370HR`
3. `Isola FR408`
4. `Isola FR408HR`

Why these four:

- they recur in safe-wave material framing
- current corpus already has official source anchors for them
- current facts still mainly use them as family-level examples or grouped anchors rather than reusable exact-product units
- they are the smallest plausible `FR-4` batch that can reduce family-shortcut pressure without reopening generic `FR-4`

Current hold on everything else:

- do not reopen already-covered `ITEQ`, `Shengyi`, or `MEGTRON` exact-product rows
- do not turn lower-loss ladder work into another generic material sweep

## Lane B: Flex-Material Exact-Product Normalization

Current primary conclusion:

- the repo is no longer truly `Panasonic-only`
- `Arlon 85NT` is already a usable exact-product polyimide anchor
- `Arlon 85N` exists as a second exact-product numeric anchor, but is lower-priority than `85NT` for safe-wave integration

Current action shape:

- prefer normalization and pack integration before creating new cards
- keep `55NT` outside the current flex-material supplementation lane because it belongs to a controlled-CTE / SMT branch, not the current flex-material branch

Immediate low-risk move:

- carry `Arlon 85NT` into the `14-layer` evidence-pack traceable input and usable-facts wording so the pack no longer understates the existing exact-product layer

## Lane C: Remaining Safe-Wave Named-Product Gap Scan

Current secondary candidates discovered in safe-wave blog demand:

- `Shengyi S1150G`
- `Kapton HN`
- `UPILEX-S`
- `ITEQ IT-180GF`
- `Shengyi S1141`
- `ITEQ IT-140`
- `ITEQ IT-158`

Current disposition:

- these are not the first batch
- they should not be mixed into the initial `NQ-4` landing until the higher-payoff `FR-4` and `Arlon` normalization work is settled
- some of them may remain held because their current value is mostly demand-side mention rather than immediate evidence-pack payoff

## Explicit Exclusions

Do not let `NQ-4` drift into:

- generic `FR-4` reopening
- generic `RCC / FRCC / build-up` reopening
- generic rigid-flex material reopening
- capability, standards, SI, PDN, thermal, or reliability numeric recovery
- branch-level readiness claims inferred from narrow exact-product exceptions

## Current Execution Order

1. land the `NQ-4` planning note and tracking updates
2. make one low-risk normalization improvement by integrating `Arlon 85NT` into `14-layer` pack wording
3. admit the `Isola IS410 / 370HR / FR408 / FR408HR` batch as the first `NQ-4` exact-product micro-batch
4. keep the remaining named-product gaps as a secondary candidate list, not as automatic batch scope

## Landed First Micro-Batch

The first `NQ-4` micro-batch is now landed:

- `facts/materials/isola-is410.md`
- `facts/materials/isola-370hr.md`
- `facts/materials/isola-fr408.md`
- `facts/materials/isola-fr408hr.md`

Current effect:

- the safe-wave program now has reusable exact-product units for the main Isola `FR-4 / high-Tg` examples already recurring in pack-level material framing
- `FR408` and `FR408HR` remain conservative product anchors even where the current source summary did not justify aggressive numeric extraction
- `370HR` now has an explicit product card that preserves the stronger condition-sensitive `Dk / Df` boundary instead of leaving that product only in grouped facts
- `IS410` now has a standalone baseline card rather than only family-ladder treatment

## Secondary Reassessment Result

The first micro-batch is now followed by a narrow reassessment pass on lower-priority named-product candidates.

Current result:

- `Kapton HN`: hold
- `UPILEX-S`: hold
- `Shengyi S1150G`: hold
- `Shengyi S1141`: hold
- `ITEQ IT-180GF`: close as not needed
- `ITEQ IT-140`: close as not needed
- `ITEQ IT-158`: close as not needed

Why these remain held:

- current safe-wave evidence packs do not gain enough immediate payoff from admitting them
- `Kapton HN` and `UPILEX-S` would create more pressure to reopen generic rigid-flex `polyimide / Kapton / UPILEX` branches than they would resolve exact-product gaps
- `S1150G` is still better treated as a secondary named-product candidate than as an automatic second-batch admission
- `S1141` is still better treated as a secondary Shengyi named-product candidate than as an automatic second-batch admission
- `IT-180GF`, `IT-140`, and `IT-158` do not currently beat the existing `ITEQ` exact-product ladder in safe-wave payoff, so they should be closed rather than kept as open card-pressure

Current queue posture after this reassessment:

- the first `NQ-4` micro-batch should be treated as complete
- follow-on named-product gaps remain reassessment-only until a later pack shows stronger exact-product need
- `NQ-4` should stay narrow and should not drift into a broad material sweep

## Success Condition

`NQ-4` succeeds if it does one of these cleanly:

1. lands a very small exact-product batch with real safe-wave payoff
2. lands normalization / pack-integration fixes that prove part of the queue was already materially covered
3. closes candidate names as held rather than creating low-signal cards

## One-Line Control Posture

- `NQ-4` should now be treated as a narrow exact-product and normalization queue whose first landed micro-batch is `Isola IS410 / 370HR / FR408 / FR408HR`, while any follow-on named-product gaps remain held until a separate payoff check justifies another batch
