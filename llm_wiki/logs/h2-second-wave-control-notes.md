# H2 Second Wave Control Notes

Date: 2026-04-25

## Purpose

This note tells the main agent how to reflect the second H2 intake wave across `backdrill_and_stub`, `registration`, and `board_thickness` without overstating progress.

Current true posture:

- `impedance_tolerance` remains the first tolerance bucket
- `trace_space`, `drill_and_via_geometry`, and `aspect_ratio` remain the first geometry-wave intake
- `backdrill_and_stub`, `registration`, and `board_thickness` are now added as a second H2 intake wave
- none of these second-wave buckets is numerically unlocked
- no second-wave capability fact card should be implied yet

## Backlog Wording

Recommended wording:

- `H2 Capability Bucket Recovery`
  - `status: now`
  - `active buckets: impedance_tolerance, first geometry wave, and second-wave intake for backdrill_and_stub / registration / board_thickness`
  - `goal: source mapping, claim splitting, and dated-record intake discipline before any numeric promotion`

## Phase-Status Wording

Recommended wording:

- `H2 capability bucket recovery now also includes a second intake wave across backdrill_and_stub, registration, and board_thickness`
- `Current work remains governance expansion, bucket decisioning, and source mapping; reusable numerics for all three second-wave buckets remain blocked`
- `Existing method and wiki pages continue to support posture and non-claim boundaries only`

## Update-Log Entry Language

Recommended entry:

- `Expanded H2 bucket intake beyond the first geometry wave by adding bucket-decision and source-map controls for backdrill_and_stub, registration, and board_thickness; this step added governance and source-routing coverage only, and did not unlock any reusable capability numbers or capability fact cards`

Optional second sentence:

- `Current internal drilling, multilayer, rigid-flex, and stackup pages remain valid for posture and boundary control, but not as transferable numeric authorities`

## Non-Claim Guardrail

Control docs must still say clearly:

- second-wave buckets are now in intake
- zero second-wave buckets are numerically unlocked
- no current internal drilling, multilayer, high-layer, rigid-flex, or stackup page should be read as a reusable capability table
- second-wave intake does not mean `backdrill_and_stub`, `registration`, or `board_thickness` recovery is complete

## Recommended Next Sequence After This Wave

After the second H2 wave, keep these held:

- `annular_ring`
  - mixed with standards-threshold risk
- `copper_plating_process_windows`
  - still requires narrower bucket split before safe intake
- `stackup_recipe_and_process_count_numbers`
  - still mixed with recipe/example leakage
- dynamic commercial numerics
  - remain excluded from H2 capability recovery

## Minimal One-Line Control Posture

- `H2 now includes second-wave intake across backdrill_and_stub, registration, and board_thickness, but all reusable numerics for those buckets remain blocked pending dated capability-source records`
