# H2 Geometry Wave Control Notes

Date: 2026-04-25

## Purpose

This note tells the main agent how to reflect the current geometry-wave expansion across multiple `H2` buckets without overstating progress.

Current true posture:

- `impedance_tolerance` remains the first active tolerance bucket
- `trace_space`, `drill_and_via_geometry`, and `aspect_ratio` are now in geometry-wave intake
- none of these geometry buckets is numerically unlocked
- no geometry capability fact card should be implied yet

## Backlog Wording

Recommended wording:

- `H2 Capability Bucket Recovery`
  - `status: now`
  - `active buckets: impedance_tolerance plus geometry-wave intake for trace_space / drill_and_via_geometry / aspect_ratio`
  - `goal: source mapping, claim splitting, and dated-record intake discipline before any numeric promotion`

## Phase-Status Wording

Recommended wording:

- `H2 capability bucket recovery now includes a geometry-wave intake across trace_space, drill_and_via_geometry, and aspect_ratio, alongside the first impedance_tolerance bucket`
- `Current work is governance expansion, bucket decisioning, and source mapping; reusable geometry numerics remain blocked`
- `Existing method and wiki pages continue to support posture and non-claim boundaries only`

## Update-Log Entry Language

Recommended entry:

- `Expanded H2 bucket intake beyond impedance_tolerance into a first geometry wave covering trace_space, drill_and_via_geometry, and aspect_ratio; this step added bucket-governance and source-mapping controls only, and did not unlock any reusable geometry numbers or capability fact cards`

Optional second sentence:

- `The current corpus remains sufficient for manufacturability framing and boundary control, but not for transferable geometry capability numerics`

## Non-Claim Guardrail

Control docs must still say clearly:

- multiple geometry buckets are now in intake
- zero geometry buckets are numerically unlocked
- no current internal drilling, HDI, or high-layer page should be read as a reusable capability table
- geometry-wave intake does not mean `trace_space`, `drill_and_via_geometry`, or `aspect_ratio` recovery is complete

## Recommended Next Sequence After This Wave

After the first geometry wave, keep the next sequence:

1. `backdrill_and_stub`
2. `registration`
3. `board_thickness`

Keep these held after that, not pulled forward:

- `annular_ring`
  - mixed with standards-threshold risk
- `copper_plating_process_windows`
  - still requires narrower bucket split before safe intake

## Minimal One-Line Control Posture

- `H2 now has a first geometry-wave intake across trace_space, drill_and_via_geometry, and aspect_ratio, but all geometry numerics remain blocked pending dated capability-source records`
