# H4 Minimum Laser Via Routing Note

Date: 2026-04-26

## Purpose

This note defines the single-class routing posture for `minimum laser via` inside `H4` shared `B` capability-parameter lane `Phase A`.

It exists to fix:

- where current demand comes from
- what blocker shape prevents reusable numeric recovery
- what authority would be acceptable in principle
- what refusal posture applies if acceptable authority is still missing

This file is not:

- a numeric recovery result
- a recovered capability table
- permission to reconstruct HDI geometry defaults
- proof that public reusable numeric readiness has improved

## First Named Blocked Branches

The first named blocked branches for this parameter are:

- `20-layer`
- `22-layer`

They remain first because `minimum laser via` pressure still concentrates most strongly in:

- `20-layer` HDI / build-up vocabulary
- `22-layer` high-density manufacturability pressure
- repeated collapse from architecture language into unsupported geometry proof

## Main Demand Branches

Current main demand branches:

- `20-layer`
- `22-layer`
- `18-layer`

Secondary downstream reuse pressure may later appear for adjacent high-density branches, but that is not a current unlock path.

## Current Blocker Shape

The current blocker shape for `minimum laser via` is:

1. public `any-layer`, `ELIC`, microvia, and build-up vocabulary repeatedly gets overextended into unsupported geometry authority
2. supplier-scoped microvia capability claims are especially easy to mistake for shared reusable numeric authority
3. method identity, material identity, and actual laser-via capability identity are often collapsed into one unsupported minimum
4. current draft demand creates repeated pressure for a compact number even when no acceptable shared authority exists

## Acceptable Authority Requirement

`Minimum laser via` should be considered for reusable numeric handling only if all of the following are true:

1. a primary source exposes the parameter directly as a numeric field
2. the source has explicit scope and condition
3. the source separates method or architecture vocabulary from actual capability identity
4. the source is strong enough to show whether the number is public-general, product-bound, process-bound, supplier-scoped, or date-bound

Additional rule:

- public `any-layer`, `ELIC`, stacked-microvia, or build-up vocabulary does not by itself qualify as reusable numeric authority
- supplier-scoped proof records, if they appear later, remain supplier-scoped and dated unless a separate later workflow explicitly says otherwise
- marketing summaries do not qualify as shared reusable numeric authority

## Default Refusal Posture

If acceptable authority is missing:

- refuse numeric recovery for `minimum laser via`
- keep only architecture, build-up, or method-name context
- route supplier-specific microvia capability wording to `hold_for_supplier_evidence`
- do not convert density pressure into a shared geometry table

## Non-Reuse Boundary

The following do not qualify by default as reusable numeric authority for this parameter:

- supplier-scoped proof pages
- factory marketing summaries
- public `HDI` / `any-layer` / `ELIC` vocabulary pages
- broad process-overview pages
- derivative comparison tables
- draft demand from `20-layer`, `22-layer`, or adjacent blogs

Rule:

- these source shapes may help boundary control or workflow wording
- they must not be rewritten into a shared `minimum laser via` numeric class

## Minimal Outcome

This routing note is successful if future controller work can keep `minimum laser via` in one of the following stable postures:

- `refuse_numeric_recovery`
- `keep_non_numeric_context_only`
- `hold_for_supplier_evidence`

The intended outcome is routing stability, not geometry recovery.

## Readiness Unchanged

Public reusable numeric readiness remains unchanged after this note.

This file should not be interpreted as:

- a recovered laser-via minimum
- a shared authority unlock
- permission to reuse supplier-scoped proof as public numeric evidence
- permission to turn architecture vocabulary into capability numerics
