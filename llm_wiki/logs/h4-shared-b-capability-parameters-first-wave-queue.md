# H4 Shared B Capability Parameters First-Wave Queue

Date: 2026-04-26

## Purpose

This note converts the `H4` kickoff into the first execution-grade queue for shared `B` capability parameters.

It exists to define the smallest reusable numeric-class batch worth attempting first across the still-blocked high-density branches.

This file is:

- a queue/control note
- a first-wave recovery order
- a bounded multi-branch demand map

This file is not:

- a numeric recovery result
- a permission note for restoring blocked capability tables
- proof that public reusable numeric readiness has improved

## Why This Queue Starts Here

The shortest path toward `more numbers` is not to reopen all shared `B` classes at once.

The shortest path is to start with the smallest cross-draft parameter set that:

- recurs across the blocked high-layer drafts
- has the highest chance of later serving multiple blogs
- does not automatically collapse into standards-threshold reconstruction
- does not depend by default on supplier-scoped proof claims

That first-wave set is:

1. `trace/space`
2. `minimum mechanical drill`
3. `minimum laser via`
4. `aspect ratio`
5. `annular ring`
6. `impedance tolerance`
7. `registration tolerance`

## Queue Scope

### First named blocked branches

This queue starts from:

- `20-layer`
- `22-layer`

These remain the first named blocked branches because they still concentrate the hardest shared capability-parameter pressure and the strongest risk of overclaim.

### Adjacent reuse surface

If any class in this queue later becomes reusable under acceptable evidence rules, the next likely beneficiaries are:

- `14-layer`
- `18-layer`
- `24-layer`

That possible reuse is a later payoff, not a current unlock.

## Per-Class Control Posture

### 1. `trace/space`

Reason to prioritize:

- appears repeatedly in high-layer manufacturability framing
- is one of the most common ways current drafts try to imply factory capability
- can influence whether compact capability tables are even possible later

Current posture:

- vocabulary and boundary control exist
- reusable numeric recovery remains blocked

### 2. `minimum mechanical drill`

Reason to prioritize:

- recurs across multilayer density and escape-routing language
- often gets mixed with generic vendor-capability claims

Current posture:

- process framing exists
- reusable numeric recovery remains blocked

### 3. `minimum laser via`

Reason to prioritize:

- central to HDI/build-up language in `20-layer`
- often collapses public any-layer vocabulary into unsupported factory proof

Current posture:

- HDI/process vocabulary exists
- reusable numeric recovery remains blocked

### 4. `aspect ratio`

Reason to prioritize:

- cross-links drill, plating, thickness, and manufacturability language
- often appears as a transferable shop-capability shorthand

Current posture:

- bucket governance exists
- reusable numeric recovery remains blocked

### 5. `annular ring`

Reason to prioritize:

- recurring bridge point between capability numerics and standards-threshold leakage
- affects both fabrication framing and acceptance overreach

Current posture:

- boundary pressure is known
- reusable numeric recovery remains blocked

### 6. `impedance tolerance`

Reason to prioritize:

- already has strong demand across multiple drafts
- often gets confused with verification posture or SI/channel guarantees

Current posture:

- claim-family split and posture support exist
- reusable numeric recovery remains blocked

### 7. `registration tolerance`

Reason to prioritize:

- recurring high-layer manufacturability pressure point
- often implied indirectly by stackup density or alignment wording

Current posture:

- control vocabulary exists
- reusable numeric recovery remains blocked

## What Stays Out Of First Wave

The following remain outside the first compact wave:

- board-thickness windows
- backdrill / residual-stub classes
- plating-thickness thresholds
- bow / twist thresholds
- process-window, bake-cure, or lamination-count numerics
- lead time, yield, pricing, or quick-turn numbers

They stay out because they either:

- collapse too quickly into standards-threshold recovery
- depend too heavily on supplier-scoped factory proof
- are less reusable than the first seven classes

## Required Evidence Discipline

No class in this queue should be treated as reusable unless acceptable authority actually exists.

Minimum discipline:

1. do not recover numbers from memory, derivative tables, or metadata-safe pages
2. do not convert supplier-side marketing posture into transferable capability numerics
3. do not let standards hierarchy or workflow pages backdoor threshold values
4. do not treat `20-layer` or `22-layer` draft demand as evidence

## Expected Output Shape

The intended next output is not a fact card.

The intended next output is a class-by-class queue note that, for each first-wave parameter, fixes:

- main demand branches
- current blocker shape
- acceptable-source requirement
- default refusal posture if authority is still missing

## Minimal Current Verdict

This first-wave queue means the program is now moving toward `more numbers` through the shared `B` capability-parameter path.

It does not mean any of those seven classes are already recovered.

Public reusable numeric readiness remains unchanged unless later work lands acceptable evidence.
